fabric:
  cas:
  - "ca1.vendedor.nerdstore.com.br"
  peers: 
  - "peer1.vendedor.nerdstore.com.br"
  settings:
    ca:
      FABRIC_LOGGING_SPEC: DEBUG
    peer:
      FABRIC_LOGGING_SPEC: DEBUG
    orderer:
      FABRIC_LOGGING_SPEC: DEBUG
  
  netname: "RedeBlockchainNerdStore2"
  
  container_options: "--restart=always --log-opt max-size=10m --log-opt max-file=3"
